(ns cardy.db)

; (def default-db
;   {:page :home})

; (def default-db
;   {:page :home
;    :decks '({:front "mon premier carte" :back "my first card"})})


(def default-db
  {:page :home
   :decks
        {:misc
            '({:front "mon premier carte" :back "my first card"})}})
